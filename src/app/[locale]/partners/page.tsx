import PartnersPage from '@/components/partners/PartnersPage';
import type { Metadata } from 'next';

export const metadata: Metadata = { title: 'BENEFIT — Our Partners' };

export default function Partners() {
  return <PartnersPage />;
}
